<Window x:Class="AutoPrinter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoPrinter"
        mc:Ignorable="d"
        Title="Auto Label Printer" Height="450" Width="800">


    <Grid>
        <!-- Define 2 Columns: Sidebar + Main -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <!-- Sidebar -->
            <ColumnDefinition Width="*"/>
            <!-- Main -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <DockPanel Grid.Column="0" LastChildFill="False">

            <!-- Pause/Resume Button -->
            <Button x:Name="PauseResumeBtn"
                Margin="10"
                Height="40"
                DockPanel.Dock="Top"
                Click="PauseResumeBtn_Click">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock x:Name="PauseResumeIcon" Text="⏸" FontSize="16" Margin="0,0,5,0"/>
                            <TextBlock x:Name="PauseResumeText" Text="Pause" FontSize="14"/>
                        </StackPanel>
            </Button>

            <Button x:Name="SettingsBtn"
                    Content="⚙ Settings"
                    Margin="10"
                    Height="40"
                    Click="BtnSettings_Click"
                    DockPanel.Dock="Bottom"
                    />
        </DockPanel>

        <!-- Main Content -->
        <Grid Grid.Column="1" Background="White">
            <Border Margin="10" BorderBrush="#ccc" BorderThickness="1" CornerRadius="5">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="TxtLogs"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     AcceptsReturn="True"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>
